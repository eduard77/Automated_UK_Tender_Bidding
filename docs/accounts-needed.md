# Accounts the operator must register

Running list of external services / portals that **cannot be set up automatically** —
they require the operator to register, accept terms, verify email, or arrange
2FA. Each later phase appends here whenever it discovers a blocker that fits.

| # | Service | URL | Why needed | What it unlocks | Notes |
| - | --- | --- | --- | --- | --- |

_No entries yet. Phase 0 returned NEEDS DECISION before any source investigation
ran — see `docs/build-progress.md` for the reason. Once the handover lands, Phase 1's
diagnosis pass will populate this list with any source whose silent ingest is due to
"no account yet" rather than a code bug._
